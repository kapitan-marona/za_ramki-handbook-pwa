Зачем эти файлы:

1) tasks.json — активные задачи (их видят сотрудники в вкладке "Задачи").
2) tasks_archive.json — архив (старые/завершённые задачи). Пока UI архив не показывает.

Позже будет "админка", которая позволит переносить карточки между активными и архивом без ручного редактирования JSON.
